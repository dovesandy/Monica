# PinMe 介绍和部署文档

## 介绍

PinMe 是一个免费的 IPFS 托管平台，专为静态网站部署设计。它能让开发者在几秒钟内将网站部署到 IPFS 网络上，确保内容的持久性和抗审查能力。PinMe 的核心价值是提供简单、快速、免费的前端部署体验，让开发者专注于内容创作。

### 为什么选择 PinMe？

相比传统托管服务，PinMe 具有以下优势：

- **完全免费**：无需支付服务器费用或订阅费用。
- **去中心化**：内容存储在 IPFS 网络上，不依赖单一公司服务器。
- **内容自主权**：用户完全掌控自己的内容，无平台锁定风险。
- **抗审查性强**：内容不易被单方面删除。
- **全球加速**：通过点对点网络优化全球访问速度。

### 适用场景

PinMe 适用于以下场景：

- 个人博客和作品集
- 开源项目文档
- 原型设计展示
- 小型企业网站
- 实验性项目

## 部署指南

### 前提条件

- 静态网站文件（HTML、CSS、JavaScript、图片等）。
- 不包含服务器端代码。
- 推荐使用相对路径引用资源。
- 使用框架的项目需先构建生成静态文件。

### 部署方式

#### 终端部署

1. 安装 PinMe CLI：

   ```
   npm install -g pinme
   ```

2. 部署静态网站：

   ```
   pinme upload <folder/file-path>
   ```

   其中 `<folder/file-path>` 是要部署的静态网站文件夹或文件路径。

#### 浏览器部署

PinMe 也支持通过网页直接上传文件。访问 PinMe 官网，上传您的静态文件即可。

### 常见框架部署

- **React 项目**：

  ```
  npm run build
  pinme upload build/
  ```

- **Vue 项目**：

  ```
  npm run build
  pinme upload dist/
  ```

- **Angular 项目**：

  ```
  ng build
  pinme upload dist/
  ```

### 自定义域名（可选）

1. 获取网站的 IPFS CID。
2. 在域名注册商处设置 DNS 记录：
   - 添加 CNAME 记录指向 PinMe 的 IPFS 网关。
   - 或使用 DNSLink（推荐）。
3. 详情参考 [IPFS 官方文档](https://docs.ipfs.tech/how-to/websites-on-ipfs/link-a-domain/#option-1-use-a-dns-link)。

## 工作原理

PinMe 基于 IPFS 协议工作：

1. **内容哈希生成**：上传文件时，为每个文件生成唯一的 IPFS 内容标识符（CID）。
2. **内容固定**：将内容固定在 IPFS 网络上，防止被垃圾回收。
3. **内容分发**：分发到多个节点，提高可用性和访问速度。
4. **访问路由**：提供从域名到 IPFS 内容的解析服务。

## IPFS 基础知识

### 什么是 IPFS？

IPFS（星际文件系统）是一个开源的点对点分布式文件系统，旨在创建持久且分布式的 Web。它使用基于内容寻址的方法，每个文件由其内容的哈希值标识。

### 核心概念

- **内容寻址**：使用内容的哈希值（CID）标识文件，相同内容只存储一次。
- **分布式存储**：文件分布在网络的多个节点上，提高可用性。
- **版本控制**：每次修改生成新 CID，支持历史版本追踪。
- **内容固定**："固定"内容防止被垃圾回收。

## 常见问题

- **部署失败**：检查文件是否为静态网站，确保无服务器端代码。
- **自定义域名不生效**：确认 DNS 配置正确，可能需要等待 DNS 传播。
- **网站加载慢**：检查文件大小，优化图片和资源。
- **内容删除**：IPFS 内容不易删除，但需注意避免存储敏感信息。

## 联系我们

如果您有任何问题，请访问 [PinMe 支持页面](https://pinme.com/support) 或发送邮件至 <support@pinme.com>。

## 修改说明

本文档基于掘金文章 [前端部署工具 PinMe](https://juejin.cn/post/7555271555837739008) 进行了更新。主要修改包括：

- 更新介绍部分，强调 PinMe 基于 IPFS 的去中心化特性。
- 修正部署方式，添加 CLI 和浏览器部署的具体步骤。
- 增加常见框架部署示例。
- 添加工作原理和 IPFS 基础知识章节。
- 更新常见问题，添加 IPFS 相关注意事项。

---

*本文档基于掘金文章编写。如有最新信息，请参考官方文档。*
